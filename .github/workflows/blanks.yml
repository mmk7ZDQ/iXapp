name: Android CI (Blank Template)

# 触发条件，可以自己修改
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build APK (Blank)
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    # 下面这些步骤是空白占位，后续可以填充 Android SDK / Gradle 构建
    - name: Set up environment
      run: |
        echo "这里可以设置 JDK、Android SDK 或其他环境变量"

    - name: Build APK
      run: |
        echo "这里填充 ./gradlew assembleDebug 或其他构建命令"

    - name: Upload APK artifact
      run: |
        echo "这里上传构建好的 APK"
